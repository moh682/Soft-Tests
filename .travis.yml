language: node_js
node_js:  
  - "node"

services:
  - mysql

env: TEST_MYSQL_USERNAME=root TEST_MYSQL_PASSWORD="" DATABASE="test_backend"

stages:
  - build

jobs:
  include: 
      stage: build
      env: TEST_MYSQL_USERNAME=root TEST_MYSQL_PASSWORD="" DATABASE="test_backend" 
      script: 
        - cd assignments/Assignment4
        - npm i;
        - mysql -u root < ./sql/sql.sql
        - npm run test 